<div class="navbar-fixed">
  <nav>
    <div class="nav-wrapper">
      <!-- <a href="#" class="brand-logo">Logo</a> -->
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <% if !user_signed_in? %>
          <li><%= link_to "The Wall", root_path %></li>
        <% end -%>
        <li><%= link_to "About", about_path %></li>
        <% if user_signed_in? %>
          <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Log In", new_user_session_path, class: 'button' %></li>
        <% end -%>
      </ul>
      <ul id="nav-mobile" class="left hide-on-med-and-down">
        <%if user_signed_in? %>
          <li>
            <a class="dropdown-button" href="#!" data-activates="mydropdown">Profile<i class="material-icons right">arrow_drop_down</i></a>
            <script>
              $(".dropdown-button").dropdown();
            </script>
          </li>
        <% end -%>
      </ul>
      <% if user_signed_in? %>
        <ul class="dropdown-content" id="mydropdown">
          <li><%= link_to "Profile", user_path(current_user.id) %></li>
          <li><%= link_to "My feed", myfeed_path %></li>
          <li class="divider"></li>
          <li><%= link_to "The Wall", root_path %></li>
        </ul>
      <% end -%>
    </div>
  </nav>
</div>
